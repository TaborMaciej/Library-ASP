@model IEnumerable<Library_project.Models.Ulica>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KodPocztowy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miasto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KodPocztowy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Miasto.IDMiasto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDMiasto">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.IDMiasto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
